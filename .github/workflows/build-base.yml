name: Build Base Images

on:
  push:
    branches: [main]
    paths:
      - '9/Containerfile'
      - '9/rockylinux-9.yaml'
      - '10/Containerfile'
      - '10/rockylinux-10.yaml'
      - '.github/workflows/build-base.yml'
  pull_request:
    branches: [main]
    paths:
      - '9/Containerfile'
      - '9/rockylinux-9.yaml'
      - '10/Containerfile'
      - '10/rockylinux-10.yaml'
      - '.github/workflows/build-base.yml'
  workflow_dispatch:
    inputs:
      rocky_version:
        description: 'Rocky Linux version to build (9, 10, or all)'
        required: false
        default: 'all'

env:
  IMAGE_NAME: rocky-bootc
  REGISTRY: ghcr.io

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        rocky_version: [9, 10]
    steps:
      - name: Filter by input (workflow_dispatch)
        if: >-
          github.event_name == 'workflow_dispatch' &&
          github.event.inputs.rocky_version != 'all' &&
          github.event.inputs.rocky_version != matrix.rocky_version
        run: |
          echo "Skipping Rocky ${{ matrix.rocky_version }} (requested ${{ github.event.inputs.rocky_version }})"
          exit 0

      - name: Checkout
        uses: actions/checkout@v4

      - name: Install podman
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq podman fuse-overlayfs

      - name: Build base image (Rocky ${{ matrix.rocky_version }})
        run: |
          sudo podman build \
            --platform=linux/amd64 \
            --security-opt=label=disable \
            --cap-add=all \
            --device /dev/fuse \
            -t ${{ env.IMAGE_NAME }}:${{ matrix.rocky_version }} \
            -f ${{ matrix.rocky_version }}/Containerfile \
            .

      - name: Verify image
        run: |
          sudo podman inspect ${{ env.IMAGE_NAME }}:${{ matrix.rocky_version }} \
            --format '{{.Id}} {{.Size}}'

      - name: Login to GHCR
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | \
            sudo podman login ${{ env.REGISTRY }} \
              -u ${{ github.actor }} --password-stdin

      - name: Push to GHCR
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          FULL_IMAGE=${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}
          sudo podman tag \
            ${{ env.IMAGE_NAME }}:${{ matrix.rocky_version }} \
            ${FULL_IMAGE}:${{ matrix.rocky_version }}
          sudo podman push ${FULL_IMAGE}:${{ matrix.rocky_version }}
